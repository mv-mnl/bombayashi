# -----------------------------------------------------
# System Config
# -----------------------------------------------------

bind = $mainMod SHIFT, M, exit, 
bind = $mainMod SHIFT, R, exec, hyprctl reload
bind = $mainMod SHIFT, Q, exec, shutdown now
bind = $mainMod, SPACE, exec, ~/.config/hypr/scripts/cambioTeclado.sh
bind = $mainMod, L, exec, hyprlock

